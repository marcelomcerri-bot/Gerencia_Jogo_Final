{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "PORT=3000 BASE_PATH=/ npm run dev -w @workspace/gestor-enf",
    "build": "npm run build -w @workspace/gestor-enf && rm -rf dist && cp -r artifacts/gestor-enf/dist/public dist"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "framer-motion": "^12.38.0",
    "lucide-react": "^1.8.0",
    "react-router-dom": "^7.14.2",
    "tailwind-merge": "^3.5.0"
  }
}
